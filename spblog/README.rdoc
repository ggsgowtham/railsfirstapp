=== How the make the application work ===


Download the application 
  install the bundle using 
    "bundle install" 
  create the schema using 
    "rake db :migrate" 

Run the application using 
  rails s
